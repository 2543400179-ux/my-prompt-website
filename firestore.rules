rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ONLY allow access if the user is authenticated AND their email matches your QQ mailbox
      allow read, write: if request.auth != null && request.auth.token.email == '2543400179@qq.com';
    }
  }
}
